# Server
PORT=3000
NODE_ENV=development

# Last.fm API
LASTFM_API_KEY=your_lastfm_api_key

# Discogs API
DISCOGS_KEY=your_discogs_key
DISCOGS_SECRET=your_discogs_secret

# Anthropic API (for AI-powered personality headlines)
# Optional - falls back to template-based headlines if not set
ANTHROPIC_API_KEY=your_anthropic_api_key

# ─── Security Settings ───────────────────────────────────────────────────────

# Global API Rate Limiting (applies to /api/* routes only, not static files/pages)
# A single page load with 12 artists triggers ~60+ API calls, so this must
# accommodate normal browsing while blocking automated abuse (bots, DDoS)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=300

# AI Rate Limiting (stricter limits for /api/personality - AI calls are expensive)
AI_RATE_LIMIT_WINDOW_MS=60000
AI_RATE_LIMIT_MAX_REQUESTS=10

# CORS Allowed Origins (comma-separated list)
# In production, set this to your actual domain(s)
# Example: https://music.payamyousefi.com,https://www.music.payamyousefi.com
# In development, localhost origins are automatically allowed
ALLOWED_ORIGINS=
