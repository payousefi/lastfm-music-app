# Robots.txt for Music App
# https://music.payamyousefi.com

User-agent: *

# Allow crawling of user profile pages
Allow: /

# Disallow API endpoints (not useful for search engines)
Disallow: /api/

# Disallow server/source files (shouldn't be accessible anyway)
Disallow: /server/
Disallow: /scripts/
Disallow: /node_modules/

# Allow static assets (CSS, images)
Allow: /stylesheets/
Allow: /img/

# Sitemap (optional - add if you create one)
# Sitemap: https://music.payamyousefi.com/sitemap.xml
